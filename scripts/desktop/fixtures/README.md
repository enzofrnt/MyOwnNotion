# Desktop installed-smoke fixtures

Place non-secret fixtures for native runners here (sample manifest JSON,
unsigned layout probes). Signing certificates and notarization credentials
must never be copied into this directory; they arrive as runner secrets.
