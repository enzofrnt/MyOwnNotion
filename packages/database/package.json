{
  "name": "@myownnotion/database",
  "version": "0.1.0",
  "private": true,
  "description": "PostgreSQL canonical store: Drizzle schema, reviewed SQL migrations, transactional repositories",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@myownnotion/domain": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@myownnotion/test-utils": "workspace:*",
    "@types/pg": "^8.15.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
