/**
 * Encrypted chunk storage (feature 002).
 *
 * Content-addressed encrypted chunks and their envelope metadata. Plaintext
 * source cleanup belongs to migration orchestration, not to this package.
 */

export {};
