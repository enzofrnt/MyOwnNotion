{
  "name": "@myownnotion/test-utils",
  "version": "0.1.0",
  "private": true,
  "description": "Shared test fixtures: disposable PostgreSQL, and fast-check generators for the block content model",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./documents": "./src/document-arbitrary.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@myownnotion/database": "workspace:*",
    "@myownnotion/domain": "workspace:*",
    "@testcontainers/postgresql": "^11.11.1",
    "fast-check": "^4.5.3",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.6",
    "typescript": "^5.9.3"
  }
}
