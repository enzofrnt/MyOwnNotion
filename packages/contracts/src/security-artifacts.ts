/**
 * Versioned security artifact schemas (feature 002).
 *
 * Mirrors `specs/002-owner-security-foundation/contracts/security-artifacts.schema.json`:
 * the `mn.enc.v1` envelope, pending bootstrap credential material, recovery
 * kits, rotation manifests, and migration checkpoints. The recovery artifact
 * uses two independent axes — `authorizationState` and `deliveryState` — with
 * no mixed `state` field. Defined by T014.
 */

export {};
