/**
 * Security API DTOs, problem envelopes, and runtime validators (feature 002).
 *
 * Mirrors `specs/002-owner-security-foundation/contracts/security-api.openapi.yaml`.
 * Cookie metadata, the response-only `X-Bootstrap-Capability` and
 * `X-CSRF-Token` handling, pending-credential and bootstrap-confirmation
 * response types, and nullable device timestamps are defined by T013.
 */

export {};
