{
  "name": "@myownnotion/graph",
  "version": "0.1.0",
  "private": true,
  "description": "Pure, deterministic knowledge-graph projections over canonical workspace data",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@myownnotion/domain": "workspace:*"
  },
  "devDependencies": {
    "fast-check": "^4.5.3",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
